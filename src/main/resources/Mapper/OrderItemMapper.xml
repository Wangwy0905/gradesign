<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.libei.mapper.OrderItemMapper">

    <select id="queryAll" resultType="OrderItem">
        select id,cost,user_id userId,product_id productId,quantity,create_date createDate from order_detail where 1=1  order by create_date desc
    </select>
   <!-- <delete id="deleteByUserId">
        delete from order_entity where user_id =#{userId}
    </delete>

    <select id="queryFront" resultType="java.lang.Long">
        select id,cost,number,user_id userId,address_id addressId,status,create_date createDate from order_entity where 1=1 and user_id =#{userId} order by create_date desc
    </select>-->

</mapper>
