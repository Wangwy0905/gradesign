<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.wenyi.mapper.AdminMapper">

    <select id="totalNum" resultType="java.lang.Integer">

    select count(*) from admin

</select>
    <select id="queryAll" resultType="Admin">

        select adminId,username,password,grade,salt from admin limit ${(page-1)*rows},${rows}

    </select>
</mapper>